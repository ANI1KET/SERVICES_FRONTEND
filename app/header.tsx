"use client";

// import useBreakpoint from "@/app/utils/useBreakpoint";

const Header = () => {
  //   const { isMobile,isDesktop,isLaptop } = useBreakpoint();

  return <div className={`h-[10vh] fixed left-0 right-0 bg-black`}></div>;
};

export default Header;
