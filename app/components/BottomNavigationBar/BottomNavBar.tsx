'use server';

import BottomBar from './BottomBar';

const BottomNavBar = () => {
  return (
    <div className="hidden max-sm:block">
      <BottomBar />
    </div>
  );
};

export default BottomNavBar;
